package AccessModifiers;

public class AccessModifiers {

	public static void main(String[] args) {
	     // Access Modifiers in Java using Encapsulation
		 // 1. Private -> Only within a particular Class not outside that Class
		 // 2. Default -> Restricted till our (one) Package. Not outside a Package. Do not specify anything for using Default keyword. It means the same.
		 // 3. Protected -> Can be accessed within the package and outside the package by a Child Class.If child class is not made, then can not be accessed outside the Package 
		 // 4. Public -> Everywhere accessible. In class, outside class, outside packages everywhere.
    
	}
}
